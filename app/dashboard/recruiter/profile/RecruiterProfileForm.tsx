"use client"; import { useState } from "react";
export function RecruiterProfileForm({ profile }: { profile: { companyName: string; industry: string; website: string; phone: string } }) { const [form, setForm] = useState(profile); const [message, setMessage] = useState(""); const [saving, setSaving] = useState(false); const change = (key: keyof typeof form, value: string) => setForm((current) => ({ ...current, [key]: value })); async function submit(e: React.FormEvent) { e.preventDefault(); setSaving(true); const response = await fetch("/api/recruiter-profile", { method: "PUT", headers: { "Content-Type": "application/json" }, body: JSON.stringify(form) }); const data = await response.json(); setMessage(response.ok ? "Company profile saved." : data.error || "Unable to save."); setSaving(false); } return <form onSubmit={submit} className="grid gap-5 rounded-2xl border bg-white p-6 dark:bg-gray-900 md:grid-cols-2"><label className="grid gap-1 text-sm font-semibold">Company name<input required value={form.companyName} onChange={(e) => change("companyName", e.target.value)} className="rounded-lg border p-2 font-normal dark:bg-gray-800" /></label><label className="grid gap-1 text-sm font-semibold">Industry<input value={form.industry} onChange={(e) => change("industry", e.target.value)} className="rounded-lg border p-2 font-normal dark:bg-gray-800" /></label><label className="grid gap-1 text-sm font-semibold">Website<input type="url" value={form.website} onChange={(e) => change("website", e.target.value)} placeholder="https://" className="rounded-lg border p-2 font-normal dark:bg-gray-800" /></label><label className="grid gap-1 text-sm font-semibold">Company phone<input value={form.phone} onChange={(e) => change("phone", e.target.value)} className="rounded-lg border p-2 font-normal dark:bg-gray-800" /></label><div className="md:col-span-2 flex gap-3"><button disabled={saving} className="rounded-lg bg-purple-600 px-5 py-2.5 text-sm font-bold text-white disabled:opacity-60">{saving ? "Saving..." : "Save company profile"}</button>{message && <p className="self-center text-sm text-gray-600">{message}</p>}</div></form>; }
