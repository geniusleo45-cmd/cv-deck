import { prisma } from "@/lib/prisma";
import { requireAdmin } from "@/lib/rbac";
export default async function AdminAnalyticsPage() { await requireAdmin(); const [orders, payments, products, vendors] = await Promise.all([prisma.order.count(), prisma.payment.aggregate({ where: { status: "SUCCESS" }, _sum: { amount: true } }), prisma.product.count({ where: { status: "ACTIVE" } }), prisma.vendor.count({ where: { status: "VERIFIED" } })]); const cards = [["Paid revenue", `₦${(payments._sum.amount || 0).toLocaleString()}`], ["Orders", orders], ["Active products", products], ["Verified vendors", vendors]]; return <section className="space-y-6"><div><h1 className="text-2xl font-black">Analytics & reports</h1><p className="mt-1 text-sm text-gray-500">Live marketplace performance summary.</p></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([label, value]) => <div key={String(label)} className="rounded-2xl border bg-white p-5 dark:bg-gray-900"><p className="text-sm text-gray-500">{label}</p><p className="mt-2 text-2xl font-black">{value}</p></div>)}</div></section>; }
