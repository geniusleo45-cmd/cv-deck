"use client";
import { useState } from "react";
import { Flag } from "lucide-react";
export function ReportButton({ targetType, targetId }: { targetType: "PRODUCT" | "VENDOR"; targetId: string }) { const [message, setMessage] = useState(""); const report = async () => { const reason = window.prompt("Tell us what is wrong (at least 8 characters):"); if (!reason) return; const response = await fetch("/api/reports", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ targetType, targetId, reason }) }); const data = await response.json(); setMessage(response.ok ? "Report submitted." : data.error || "Unable to submit report."); }; return <span className="inline-flex flex-col items-end"><button type="button" onClick={report} className="inline-flex items-center gap-1 text-xs font-semibold text-gray-500 hover:text-red-600"><Flag className="h-3.5 w-3.5" /> Report</button>{message && <span className="mt-1 text-[10px] text-gray-500">{message}</span>}</span>; }
